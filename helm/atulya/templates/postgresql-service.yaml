{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "atulya.fullname" . }}-postgresql
  labels:
    {{- include "atulya.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.postgresql.service.port }}
    targetPort: postgresql
    protocol: TCP
    name: postgresql
  selector:
    {{- include "atulya.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
{{- end }}
