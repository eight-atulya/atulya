{{- if .Values.tei.embedding.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "atulya.fullname" . }}-tei-embedding
  labels:
    {{- include "atulya.tei.embedding.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.tei.embedding.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "atulya.tei.embedding.selectorLabels" . | nindent 4 }}
{{- end }}
