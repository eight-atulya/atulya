{{- if .Values.controlPlane.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "atulya.fullname" . }}-control-plane
  labels:
    {{- include "atulya.controlPlane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.controlPlane.service.type }}
  ports:
  - port: {{ .Values.controlPlane.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "atulya.controlPlane.selectorLabels" . | nindent 4 }}
{{- end }}
